// var app = angular.module('PwCWorkFlow', []);

app.registerCtrl('headerController', ['$scope', '$http', function($scope, $http) {

    //the process for the header part

    $scope.userName = "admin";
    //user manager for user

}]);
